const subt = (a, b) => a - b;
const mult = (a, b) => a * b;

const userInfo = {
  username: "testz",
  password: "testz",
};

const test = mult(3, 12);

console.log(userInfo);
console.log(test);

export default (a, b) => a + b;
